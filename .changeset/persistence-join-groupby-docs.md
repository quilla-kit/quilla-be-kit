---
"@quilla-be-kit/persistence": patch
---

Document `SqlQueryBuilder` gaps: `.join()` (signature, example, column-qualification guidance), `.groupBy()` (example with aggregation + JOIN), `.from()` alias syntax, and `.select()` qualified / wildcard / pre-aliased column forms. All four were mentioned in the method table or security note but had no dedicated section or working example.
