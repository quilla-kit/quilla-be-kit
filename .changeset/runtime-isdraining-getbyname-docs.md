---
"@quilla-be-kit/runtime": patch
---

Document `ShutdownManager.isDraining()` (with health-check example) and `ComponentRegistry.getByName(name)`. Both methods existed in source but were absent from the README API surface.
