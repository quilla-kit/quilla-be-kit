# @quilla-kit/lifecycle

Lifecycle primitives: `IDisposable` and `ShutdownManager` — a five-phase,
idempotent, time-bounded shutdown coordinator with a draining flag.

Zero runtime dependencies.

## Install

```sh
pnpm add @quilla-kit/lifecycle
```

## Status

Interface surface not yet implemented — scaffolded only.
